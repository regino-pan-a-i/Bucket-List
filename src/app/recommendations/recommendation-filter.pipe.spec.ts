import { RecommendationFilterPipe } from './recommendation-filter.pipe';

describe('RecommendationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecommendationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
